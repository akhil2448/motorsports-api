RESET + BASE
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

h1,
h2,
h3 {
  font-family: "Orbitron", sans-serif;
  letter-spacing: 1px;
}
body {
  background:
    radial-gradient(circle at top, rgba(255, 255, 255, 0.04), transparent 40%),
    #0b0b0f;
  color: white;
  font-family:
    -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  letter-spacing: 0.3px;
  -webkit-font-smoothing: antialiased;
}

.app-container {
  min-height: 100vh;
  padding: 16px;
  padding-bottom: 90px; /* more breathing room */

  background: #0b0b0f;
}

/* BOTTOM NAV - iOS STYLE */
.bottom-nav {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  height: 75px;

  background: rgba(15, 15, 20, 0.85);
  backdrop-filter: blur(12px);

  display: flex;
  justify-content: space-around;
  align-items: center;

  border-top: 1px solid rgba(255, 255, 255, 0.08);
  box-shadow: 0 -4px 20px rgba(0, 0, 0, 0.6);

  z-index: 1000;
}

/* NAV ITEM */
.nav-item {
  background: none;
  border: none;
  color: #777;

  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;

  font-size: 11px;
  font-weight: 500;

  cursor: pointer;
  padding: 6px 10px;
  border-radius: 12px;

  transition: all 0.2s ease;
}

/* ICON */
.nav-item .icon {
  font-size: 22px;
  margin-bottom: 3px;
}

/* ACTIVE TAB */
.nav-item.active {
  color: #ffffff;
  background: rgba(255, 255, 255, 0.08);
}

/* ACTIVE ICON POP */
.nav-item.active .icon {
  transform: scale(1.1);
}

/* SERIES CARD */
.series-card {
  position: relative;

  background: linear-gradient(
    135deg,
    rgba(20, 20, 30, 0.9),
    rgba(10, 10, 18, 0.9)
  );

  backdrop-filter: blur(10px);

  border-radius: 16px;
  padding: 16px;
  margin-bottom: 14px;

  border: 1px solid rgba(255, 255, 255, 0.06);
  box-shadow: 0 6px 30px rgba(0, 0, 0, 0.6);

  transition: all 0.2s ease;
}

.series-card:hover {
  transform: translateY(-2px);
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.8);
}

.logo-container {
  display: flex;
  align-items: center;
  height: 36px; /* fixed visual area */
  overflow: hidden; /* prevents spill */
  margin-bottom: 4px;
}

.series-logo {
  height: 26px; /* base size (bigger than text) */
  width: auto;
  object-fit: contain;
  filter: brightness(1.4); /* slight boost for dark UI */
}

/* F1 */
.series-logo.f1 {
  height: 22px;
}

/* MotoGP */
.series-logo.motogp {
  height: 24px;
}

/* WRC */
.series-logo.wrc {
  height: 26px;
}

/* GTWC (small logo → boost) */
.series-logo.gtwc {
  height: 35px;
}

/* DTM (small logo → boost more) */
.series-logo.dtm {
  height: 35px;
}

/* IndyCar */
.series-logo.indycar {
  height: 26px;
}

/* 🌈 ACCENT BAR */
.accent {
  position: absolute;
  left: 0;
  top: 0;
  bottom: 0;
  width: 3px;
  border-radius: 16px 0 0 16px;
}

.accent.f1 {
  background: #e10600;
}
.accent.motogp {
  background: #00d1ff;
}
.accent.wrc {
  background: #ff6b00;
}
.accent.indycar {
  background: #ffffff;
}
.accent.gtwc {
  background: #9b59ff;
}
.accent.dtm {
  background: #ffd600;
}

/* 🔴 LIVE INDICATOR */
.live-indicator {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  color: #ff3b30;
  font-weight: 600;
}

.dot {
  width: 8px;
  height: 8px;
  background: #ff3b30;
  border-radius: 50%;
  position: relative;
  display: inline-block;
}

/* PERFECTLY CENTERED PULSE */
.dot::after {
  content: "";
  position: absolute;
  top: 50%;
  left: 50%;

  width: 8px;
  height: 8px;

  background: #ff3b30;
  border-radius: 50%;

  transform: translate(-50%, -50%);
  transform-origin: center;

  animation: pulse 1.5s infinite;
  opacity: 0.6;
}

@keyframes pulse {
  0% {
    transform: scale(1);
    opacity: 0.6;
  }
  100% {
    transform: scale(2.5);
    opacity: 0;
  }
}

/* TOUCH FEEDBACK */
.series-card:active {
  transform: scale(0.98);
}

/* HEADER */
.series-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 10px;
}

/* SERIES NAME */
.series-name {
  font-size: 13px;
  color: #aaa;
  font-weight: 600;
  letter-spacing: 1px;
}

/* DATE */
.event-date {
  font-size: 12px;
  color: #888;
}

/* EVENT NAME */
.event-name {
  font-size: 17px;
  font-weight: 600;
  margin-top: 2px;
  margin-bottom: 4px;
}

/* LOCATION */
.event-location {
  font-size: 14px;
  color: #aaa;
}

/* DATE CONTAINER */
.date-container {
  text-align: right;
}

/* BIG DATE */
.event-date-big {
  font-size: 16px;
  font-weight: 600;
}

/* COUNTDOWN */
.countdown {
  font-size: 12px;
  color: #888;
}

.countdown.ongoing {
  color: #4caf50;
  font-weight: 600;
}

/* EXPANDED CARD */
.series-card.expanded {
  border: 1px solid rgba(255, 255, 255, 0.12);
}

/* SESSIONS */
.sessions {
  margin-top: 12px;
  border-top: 1px solid rgba(255, 255, 255, 0.08);
  padding-top: 10px;
  animation: fadeIn 0.25s ease;
}

.session {
  display: flex;
  justify-content: space-between;
  font-size: 14px;
  color: #aaa;
  margin-bottom: 6px;
}

/* LIVE SESSION */
.session.live {
  color: #ffffff;
  font-weight: 600;
}

.countdown.live {
  color: #ff3b30; /* iOS red */
  font-weight: 700;
}

.session-time {
  font-size: 12px;
}

@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(-6px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* LIVE GLOW */
.countdown.live {
  text-shadow: 0 0 8px rgba(255, 59, 48, 0.6);
}
